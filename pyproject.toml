[project]
name = "ronin"
version = "0.1.0"
description = "Interacoes com a Ronin"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "web3",
    "pyyaml",
    "google-cloud-secret-manager"
]
